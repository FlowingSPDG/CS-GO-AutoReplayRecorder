module.exports = {
    gsi_auth : "SuperUtageGSIAuth", // GSI Auth token
    gotv_delay : 20 , // Delay between live-server and GOTV match ライブ試合鯖からのGOTV遅延 tv_delay
    killtime_before: 1.5 , // Time to record kill scene before kill event キル発生何秒前からハイライトを保存するか
    killtime_after : 1.5 , // Time to switch next-kill scene after kill event キル発生後何秒後次のハイライトに移行するか
}